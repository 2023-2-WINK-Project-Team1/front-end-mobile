// 학생회 primary 색상 전역변수

/* 사용법
    1. Theme.js를 import 한다.
    2. 컴포넌트에 props로 글로벌 정의한 스타일을 가져온다.
 */

const theme = {
  primary: '#005950', // 학생회 대표 색상
  red: '#D43434', // 신청취소 button 색상
  gray: '#9C9C9C', // Button disabled 색상
  lightGray: '#D9D9D9', // 반납완료 button 색상
  white: '#ffffff', // 버튼 글자 색상
  black: '#000000', // 검은색
};

export default theme;
